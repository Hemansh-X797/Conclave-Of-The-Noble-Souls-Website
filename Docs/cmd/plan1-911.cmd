default: {
    utilize badgesystem ,livestats
}

Pathway.G: {
    - left side bottom to top animation text in hero video bg
    - when video stops, new text on top, left to right animation 
},

Pathway.P: {
    - Hover effect
    - Z-Index Utilization
    - 2 Backgrounds, the lower z-index one becomes visible when hovered over only around a certian redius
}

Pathway.L: {
    no obscursion in hero video bg (only one button on bottom right which shifts video to fullscreen)
    because the video is mesmerizing, so i want nothng to taint it
}

Pathway.N: {
    - text in right side of hero video, left side is the globe, bold cinzel decorative
}

hero video colleted for:{
    news
    lorebound
    gaming-->1
    default
}